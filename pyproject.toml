[build-system]
requires = ["maturin>=1.15,<2"]
build-backend = "maturin"

[project]
name = "flybrain-game"
version = "0.1.0"
requires-python = ">=3.12,<3.15"

[tool.maturin]
manifest-path = "crates/game-python/Cargo.toml"
module-name = "flybrain_game"
