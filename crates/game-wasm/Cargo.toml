[package]
name = "flybrain-game-wasm"
version.workspace = true
edition.workspace = true
authors.workspace = true
description = "WebAssembly boundary for the Can't Swat This simulation"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
flybrain-game-core = { path = "../game-core" }
wasm-bindgen = "0.2"

[lints]
workspace = true
