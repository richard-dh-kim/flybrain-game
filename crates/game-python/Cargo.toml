[package]
name = "flybrain-game-python"
version.workspace = true
edition.workspace = true
authors.workspace = true
description = "Python boundary for the deterministic Can't Swat This simulation"

[lib]
name = "flybrain_game"
crate-type = ["cdylib", "rlib"]

[dependencies]
flybrain-game-core = { path = "../game-core" }
pyo3 = { version = "0.29.2", features = ["extension-module"] }

[lints]
workspace = true
